git installation
	$ sudo apt install git-all
	$ sudo apt-get install install-info
	
on vscode:
	extensions:
	for git :
		Git Graph
		GitLens
		GitHup Copilot
	--------------
		Material Theme
		Community Material theme
		Material Theme Icons
		Material Icon Theme	
		Dark+ Material
		
	resource: internet

------------------------------------------
…or create a new repository on the command line
	echo "# Commands-Turorials" >> README.md
	git init
	git add README.md
	git commit -m "first commit"
	git branch -M main
	git remote add origin https://github.com/Alireza-Khajehvandi/Commands-Turorials.git
	git push -u origin main

…or push an existing repository from the command line
	git remote add origin https://github.com/Alireza-Khajehvandi/Commands-Turorials.git
	git branch -M main
	git push -u origin main

	resource: github.com



